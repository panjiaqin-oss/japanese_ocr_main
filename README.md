# 第一版 release by ClaudeCode Prompt：
我想要製作一個 Chrome Extension，它的功能如下：

用戶可以使用快捷鍵或滑鼠右鍵選單來啟用 OCR，然後拖曳滑鼠繪製一個矩形來選取日文單字
日文單字選取後，畫面中會顯示它的中文翻譯，且這個單字將會被記錄在瀏覽器的本機儲存空間，顯示為一個卡片式布局的網頁。
每張卡片包含了單字本身、單字中文翻譯、詞性、日文例句、例句中文翻譯（由 AI 生成），且旁邊有一個「刪除」和「置頂」按鈕
當用戶點擊瀏覽器的擴充功能選單並選擇此工具時，瀏覽器會顯示一個面板，裡面有兩個按鈕：一個按鈕是讓用戶開啟單字列表的網頁，另一個則是開啟「設定」頁面讓用戶輸入 API Key 資訊。

如果有資訊不足的地方需要我說明的，請先詢問我。

User has answered your questions: "OCR 辨識要用哪種方式實作？"="讓 AI（如 Claude/GPT）直接看圖", "翻譯與例句生成要用哪家 AI？"="讓用戶在設定頁選擇", "單字翻譯結果要以什麼方式顯示？"="頁面上浮動小卡", "OCR 觸發後的選取區域互動方式？"="全頁截圖+框選區域". You can now continue with the user's answers in mind.
